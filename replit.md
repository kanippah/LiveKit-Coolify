# LiveKit Server

Production LiveKit deployment on Coolify.

**Files:**
- `docker-compose.yml` - Deployment config
- `.env.example` - Example env vars
- `README.md` - Instructions

**Deploy to:** Coolify (Docker Compose)
**Endpoint:** `wss://livekit.yourdomain.com`
